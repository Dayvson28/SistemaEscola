@* @using SistemaEscolar.Models
@using SistemaEscolar.Services
@inject TurmaService TurmaService

<div class="container-fluid">
    <div class="row">
        <div class="col-md-9">
            <div class="p-4">
                <h3>Turmas</h3>

                <div class="row">
                    <div class="col-md-6">
                        <div class="border rounded p-3 mb-3">
                            <h5>Turmas</h5>
                            @foreach(var turma in turmasPrimeiro)
                            {
                                <div class="mb-1">@turma.Nome</div>
                            }
                        </div>
                    </div>
                </div>

                    <div class="col-md-6">
                        <div class="border rounded p-3 mb-3">
                            <h5>Estudantes por Série</h5>
                            @foreach(var estatistica in estatisticas)
                            {
                                <div class="mb-1">@estatistica.Key:@estatistica.Value</div>
                            }
                        </div>
                    </div>
            </div>

                <div class="mt-3">
                    <p>Esta tela permite visualizar as turmas e estatásticas de estudantes por série. Use o menu lateral para navegar entre as diferentes funcionalidades do sistema.</p>
                </div>

        </div>
    </div>
</div>


@code{
    private List<Turma> turmasPrimeiro = new(); // Armazena as turmas do 1� Ano
    private Dictionary<string, int> estatisticas = new(); // Armazena as estat�sticas de estudantes por s�rie

    protected override void OnInitialized() // Carrega os dados ao inicializar o componente
        {
        turmasPrimeiro = TurmaService.ObterTurmasPorSerie("1º Ano"); // Obtém as turmas do 1º Ano
        estatisticas = TurmaService.ObterEstatisticasPorSerie(); // Obt�m as estat�sticas de estudantes por série
        }
} *@